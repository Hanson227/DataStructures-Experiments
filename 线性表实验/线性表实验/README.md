#实验1  顺序表的基本操作及应用


一、实验目的
1. 掌握线性表的顺序存储结构
2．熟练掌握顺序表基本算法的实现
3. 掌握利用顺序表解决实际问题的方法和基本技巧


二、实验内容和要求
1.编写一个程序，实现顺序表的各种基本运算，本实验的顺序表元素的类型为char，在主函数中调用顺序表的基本操作完成如下操作:
（1）初始化顺序表L
（2）依次插入a、b、c、d、e
（3）输出顺序表L
（4）输出顺序表L的长度
（5）判断顺序表L是否为空
（6）输出顺序表的第3个元素
（7）输出元素a的逻辑位置
（8）在第4个元素位置上插入元素f
（9）输出顺序表L
（10）删除L的第3个元素
（11）输出顺序表L
（12）释放顺序表L
2.顺序表A中前m个元素有序，后n个元素有序，设计一个算法，使得整个顺序表有序，要求算法空间复杂度为O（1）。
3.假设有两个集合 A 和 B，分别用两个顺序表 La 和 Lb表示，即顺序表中的数据元素即为集合中的元素，利用顺序表的基本运算编写一个算法实现集合的交运算，即求一个新的集合C = A∩B。


#三、实验步骤
1.（1）参考教材完成顺序表的设计，文件名为SqList.cpp；
  （2）编写test1-1.cpp，测试顺序表的各操作，即书写main主函数，方法内部调用设计好的顺序表函数，需完成实验内容和要求中的12个操作。 

2.（1）使用已完成的顺序表的设计SqList.cpp；
  （2）新建test1-2.cpp，按实验要求编写顺序表排序算法。 算法提示：void sort（SqList *L，int m，int n）其中L为顺序表指针，m为前面的有序元素个数，n为后面的有序元素个数。
  （3）在test1-2.cpp中写main主函数，包括建立部分有序顺序表、输出顺序表、排序后再输出顺序表。 

3．（1）使用已完成的顺序表的设计SqList.cpp；
   （2）新建test1-3.cpp，按实验要求编写求两个结合交集的算法
   （3）在test1-3.cpp中写main主函数，测试上述求交集的算法
